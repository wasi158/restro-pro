/** Update with your business WhatsApp number (country code + number, no + or spaces). */
export const siteConfig = {
  whatsappNumber:
    process.env.NEXT_PUBLIC_WHATSAPP_NUMBER || "923245956121",
  businessName: "MaxDine-POS",
  whatsappGreeting:
    "Hi! Thanks for reaching out. How can we help with your restaurant POS setup?"
};
